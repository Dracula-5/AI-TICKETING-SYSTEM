[pytest]
testpaths = tests
filterwarnings =
    # SQLAlchemy uses datetime.utcnow internally as a column default callable;
    # not in our code — suppress until SQLAlchemy ships a fix.
    ignore::DeprecationWarning:sqlalchemy
